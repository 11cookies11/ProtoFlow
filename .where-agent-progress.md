# 计划：ProtoFlow 功能闭环（当前阶段不做 TCP）

## 0. 范围确认
- [x] 只做功能闭环，不处理商店物料/营销
- [x] 基于现有 UI 与功能打磨到可稳定交付
- [x] 本阶段排除 TCP 功能开发
- [x] 代理监控纳入 P0 主线

## 1. 已完成（第 1 轮）
- [x] 代理监控移除默认演示数据，改为后端真实数据驱动
- [x] 代理端口改为动态读取 `list_ports`（保留回退端口）
- [x] 代理配置保存前增加校验（端口必填、端口不可相同）
- [x] 代理参数 `parity` 统一标准枚举并兼容历史值映射
- [x] 抓包引擎连接类型兼容修正（`tcp`/`tcp-client`）
- [x] 前端构建校验通过（`npm run build`）

## 2. 已完成（第 2 轮）
- [x] 2.1 实现串口转发引擎（双串口桥接）
  - [x] 2.1.1 新增转发会话管理：创建、启动、停止、释放
  - [x] 2.1.2 支持 A->B / B->A 双向转发
  - [x] 2.1.3 端口占用、断连、权限异常时回传错误事件
- [x] 2.2 打通 UI 与转发状态
  - [x] 2.2.1 开关操作真实驱动后端启动/停止
  - [x] 2.2.2 删除代理前先停止会话并释放资源
  - [x] 2.2.3 监听 `proxy.status` 并回写 `running/stopped/error`
- [x] 2.3 代理抓包联动
  - [x] 2.3.1 转发数据发布 `proxy.data`
  - [x] 2.3.2 抓包引擎订阅 `proxy.data` 并产出 `capture.frame`

## 3. 已完成（第 3 轮）
- [x] 3.1 代理抓包方向与通道语义校准（RX/TX 与目标通道映射）
  - [x] 3.1.1 `proxy.data` 增加 `src_role/dst_role`（host/device）
  - [x] 3.1.2 抓包方向优先按角色判定，避免无目标通道时方向漂移
  - [x] 3.1.3 抓包通道在未指定目标时优先归一到 `host_port`

## 4. 已完成（第 4 轮）
- [x] 4.1 代理异常可视化增强（卡片级错误详情与重试引导）
  - [x] 4.1.1 卡片状态映射支持 `error`（图标/路由状态/文案）
  - [x] 4.1.2 卡片内展示后端错误文本（无错误时显示通用引导）
  - [x] 4.1.3 新增“一键重试”按钮，直接触发重新启动代理

## 5. 已完成（第 5 轮）
- [x] 5.1 代理状态持久化策略优化（运行中崩溃恢复/冷启动一致性）
  - [x] 5.1.1 代理对增加 `desiredActive`（持久化用户期望状态）
  - [x] 5.1.2 启动时按 `desiredActive` 尝试恢复会话，并重算 `running/stopped/error`
  - [x] 5.1.3 退出时统一 `stop_all`，避免残留会话导致状态漂移
  - [x] 5.1.4 代理配置加载时规范化状态字段，清理历史脏值

## 6. 已推进（第 6 轮）
- [x] 6.1 回归工具与流程落地（真实设备执行前置）
  - [x] 6.1.1 新增 `scripts/proxy_regression.py`（双向转发 + 事件一致性 + soak）
  - [x] 6.1.2 新增 `docs/PROXY_REGRESSION.md`（端口拓扑、命令、判定、排障）
- [ ] 6.2 真实设备回归执行
  - [x] 6.2.0 测试环境脚本化检查（`.venv`、`pyserial`、驱动检测、串口枚举）
  - [x] 6.2.0a 无驱动模拟回归通过（`proxy_regression_mock.py` 基础 + 30 轮稳定性）
  - [x] 6.2.2a 模拟异常断连通过（`-InjectDisconnect`）
  - [x] 6.2.3a 模拟 soak 稳定性通过（`-SoakSec`）
  - [ ] 6.2.1 双串口回环验证（真实端口）
  - [ ] 6.2.2 异常拔插验证（真实端口）
  - [ ] 6.2.3 长时稳定性验证（真实端口）

## 7. P0 其余项（转发闭环后）
- [x] 7.1 串口链路闭环（连接/收发/断开/异常）
  - [x] 7.1a 自动重连后的 `comm.connected` 事件同步修复
  - [x] 7.1b `comm.disconnected` 在串口/TCP关闭路径一致性修复
  - [x] 7.1c `comm_manager_regression_mock.py` 事件序列回归通过
- [x] 7.2 协议解析闭环（正常帧与异常帧提示）
  - [x] 7.2a 解析器补充异常错误码（`FRAME_TOO_SHORT`/`CRC_INVALID`/`LENGTH_INVALID`）
  - [x] 7.2b 前端按 `errors` 高亮异常帧（非仅 unknown）
  - [x] 7.2c `packet_engine_regression.py` 解析回归通过
- [x] 7.3 DSL 运行闭环（启动/停止/异常回传）
  - [x] 7.3a 修复临时 YAML 文件编码（UTF-8），避免中文环境编码异常
  - [x] 7.3b 增加运行生命周期状态信号（`__running__/__finished__/__stopped__/__error__`）
  - [x] 7.3c 前端桥接处理生命周期状态，避免仅依赖日志文本
  - [x] 7.3d `script_runner_regression.py` 回归通过
- [ ] 7.4 配置持久化闭环（重启一致、损坏回退）
- [ ] 7.5 打包运行一致性闭环（资源/版本/日志）

## 8. 交付物
- [x] 输出《功能验收清单（可逐项打勾）》
- [x] 输出《P0 回归用例（可复现步骤 + 预期）》
